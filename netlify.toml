# All Forward — Netlify config (static HTML site, no build step)
[build]
  publish = "."
  command = ""

# Serverless functions (daily disaster-alert engine lives here)
[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
